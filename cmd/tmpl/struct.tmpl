// {{.Description}}
type {{.Name}} struct {
    {{range .Values -}}
        // {{.Name}}: {{.Description}}
        {{.Name}} {{.Type}} `json:"{{.Property}}" yaml:"{{.Property}}" schema:"{{.Property}}"`
    {{end -}}
}
